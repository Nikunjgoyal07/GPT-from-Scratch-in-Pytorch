# GPT-from-Scratch-in-Pytorch
A minimal GPT language model built entirely from scratch in PyTorch — trained to generate Shakespeare-like text. Includes custom self-attention, multi-head attention, embeddings, positional encoding, and text generation loop.
